사용 언어 : HTML, CSS, JavaScript, jQuery라이브러리, (bootStrap라이브러리)
기능 : 성적의 평균을 계산하는 프로그램이다.
Todo-List

1.과목추가 버튼을 만든다.
1-1.과목을 추가하면 prompt로 과목명을 입력받고 정말 추가할 것인지 물어본다.
1-2. 성공적으로 추가하면 상자가 나타난다(날아온다)
1-3. 상자에는 과목명이 입력되어있다.

2.상자에 성적을 추가한다
2-1. 과목명 다음 줄에는 성적이 입력되어있다. 성적의 초기값은 NaN으로 한다.
2-2 상자 밑에는 과목명변경 버튼과 성적변경 버튼이 있다. - 과목명은 변경하지 않는 것으로 해도 좋다
2-2-2. 성적변경 등은 prompt로 받는다.

3. 상자 삭제 버튼을 추가한다.
3-1. (실제로 어떻게 적용될지는 모르겠으나) 상자를 삭제하면 슬라이드하면서 빈 자리를 채워야 한다.

4. 원하는 과목의 평균만 계산할 수 있다
4-1. form checked로 체크되지 않은 것은 계산에서 제외한다(체크된 것을 제외해도 좋다)

5. 결과 출력
5-1. 어딘가에(맨 아래 1줄 혹은 위에 계산기처럼) fixed로 상자를 붙여서 평균을 계산해서 출력하도록 한다.

(이하부터는 선택사항)
1. 상자의 색을 바꿀 수 있다.
1. 상자의 테두리 곡률을 바꿀 수 있다.
3.
4. checked 폼을 이용하지 않고 상자를 직접 클릭하는 것도 괜찮을 것이다
4. 비활성화된 상자는 크기를 조금 작게, 회색빛을 입히는 것도 좋다.(CSS를 활용하도록 한다)
5. 저장 버튼을 만들어서 계산한 과목과 결과값을 한쪽에 저장할 수 있도록 한다.
5. 사용자가 직접 메모할 수 있도록 배려할 수도 있다.